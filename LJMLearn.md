# LJM 배우는 법

9단계로 배우는 LiveJMark. 각 단계마다 **예제 → 설명 → 연습 문제** 순서로 진행됩니다.
Viewer 에디터에 직접 입력하고 Render 하며 따라오세요.

---

## 1단계 — 첫 문서와 페이지

LJM 문서는 `Page`로 나뉩니다. 첫 번째 페이지가 자동으로 열립니다.

```ljm
Page Home

안녕하세요, LJM입니다.

Page About

이 문서는 살아있습니다.
```

> 💡 `Page` 뒤에 오는 이름이 페이지의 이름입니다.

**연습** — `Page Profile` 페이지를 하나 더 추가해보세요.

---

## 2단계 — 글자 크기와 인용

```ljm
Page Home

(큰 제목입니다)[48]
(작은 부제목)[20]

<이 문장은 인용구 스타일로 표시됩니다>
```

| 문법 | 결과 |
|---|---|
| `(글자)[숫자]` | 숫자(px) 크기 텍스트 |
| `<글자>` | 왼쪽 라인이 있는 인용 블록 |

**연습** — 자기 이름을 60px로, 소개 문장을 인용구로 작성하세요.

---

## 3단계 — 계산

대괄호-소괄호 안에 수식을 쓰면 **결과가 렌더링**됩니다.

```ljm
Page Home

5 + 5 = [(5)+(5)]
10 × 2 = [(10)*2]
100 ÷ 4 = [(100/4)]
```

렌더 결과:

```
5 + 5 = 10
10 × 2 = 20
100 ÷ 4 = 25
```

> ⚠️ 수식은 LJM 내장 **MathParser**가 직접 계산합니다.
> JavaScript가 실행되는 것이 아니므로 안전합니다.

**연습** — `[(365)*(24)]`의 결과를 예측하고, 렌더로 확인하세요.

---

## 4단계 — 효과 (Effect)

LJM의 꽃입니다. `*[글자,효과이름]` 형식입니다.

```ljm
Page Home

*[타오른다,Fire]
*[무지개,Rainbow]
*[흔들흔들,Shake]
*[통통,Bounce]
*[빛나는,Glow]
*[둥둥,Float]
*[쿵쿵,Pulse]
*[타자기,Typing]
*[팡,Explosion]
*[파티클,Particle]
```

### 내장 효과 17종

| 효과 | 동작 | 효과 | 동작 |
|---|---|---|---|
| `Fire` | 불꽃처럼 일렁임 | `Fade` | 투명도 호흡 |
| `Steam` | 수증기처럼 흐릿하게 | `Blur` | 초점 흐림 |
| `Smoke` | 연기처럼 상승 | `Particle` | 파티클 방출 |
| `Dance` | 춤추듯 회전+점프 | `Float` | 부드럽게 부유 |
| `Wave` | 물결처럼 위아래 | `Pulse` | 심장박동처럼 확대/축소 |
| `Shake` | 격하게 떨림 | `Rotate` | 계속 회전 |
| `Glow` | 빛남 | `Rainbow` | 색상이 순환 |
| `Bounce` | 통통 튀김 | `Typing` | 한 글자씩 타이핑 |
| `Explosion` | 터지며 사라짐 | | |

**연습** — 자기 이름에 `Rainbow`와 `Glow` 중 더 어울리는 효과를 적용하세요.

---

## 5단계 — 스크립트

`([` 와 `])` 사이가 **스크립트 블록**입니다.
LJM 전용 언어로, JavaScript가 아닙니다.

```ljm
Page Home

([
var hp = 100
print("HP는 " + hp)

if hp > 50 {
  print("상태: 생존")
} else {
  print("상태: 위험")
}

repeat 3 {
  print("반복 실행!")
}

function greet {
  print("함수 호출 성공")
}

greet()
])
```

`print()`의 결과는 하단 **Console**에 출력됩니다.

### 스크립트 문법 요약

| 문법 | 예시 |
|---|---|
| 변수 선언 | `var x = 10` |
| 대입 | `x = x + 1` |
| 함수 | `function 이름 { ... }` |
| 조건문 | `if 조건 { } else { }` |
| 반복 | `repeat 5 { }` / `while 조건 { }` |
| 출력 | `print("메시지")` |
| 비교 | `>` `<` `>=` `<=` `==` `!=` |

**연습** — `var score = 85`를 선언하고, 90 이상이면 "A", 아니면 "B"를 출력하는 조건문을 만드세요.

---

## 6단계 — 타이머와 이벤트

```ljm
Page Home

([
timer 2000 {
  print("2초가 지났습니다")
}

onClick {
  print("어딘가 클릭됨!")
}

onMouseMove {
  print("마우스가 움직이는 중")
}
])
```

| 문법 | 동작 |
|---|---|
| `timer 밀리초 { }` | 지정 시간 후 1회 실행 |
| `onClick { }` | 렌더 화면 클릭 시 실행 |
| `onMouseMove { }` | 마우스 이동 시 실행 |
| `after { }` | 렌더 직후 지연 실행 |

**연습** — 3초 후에 "시간 초과!"를 출력하는 타이머를 만드세요.

---

## 7단계 — 카메라

렌더 화면 전체를 제어합니다.

```ljm
Page Home

([
camera.zoom(2)      // 2배 확대
camera.move(50, 0)  // 오른쪽으로 50px
camera.rotate(5)    // 5도 회전
])
```

**연습** — `timer`와 조합해 2초 뒤 카메라가 확대되도록 만들어보세요.

---

## 8단계 — 페이지 이동

스크립트의 `goto()`로 페이지를 전환합니다.

```ljm
Page Home

(인트로)[40]

([
timer 2000 {
  goto("Game")
}
])

Page Game

(게임 시작!)[40]
*[START,Rainbow]
```

2초 후 자동으로 Game 페이지로 이동합니다.

**연습** — Home → Game → End 로 이어지는 3페이지 문서를 만들고,
각 페이지마다 `timer`로 자동 이동하게 하세요.

---

## 9단계 — API 호출

외부 데이터를 문서로 가져옵니다.

```ljm
Page Home

<외부 데이터 불러오기>

@API@
{
  url:"https://jsonplaceholder.typicode.com/todos/1",
  method:"GET"
}
@
```

응답 결과는 문서에 바로 렌더링됩니다.

> ⚠️ CORS를 허용하는 API만 브라우저에서 직접 호출할 수 있습니다.

---

## 🏆 최종 연습 — 미니 게임 문서

아래 요소를 모두 사용해 나만의 문서를 완성해보세요.

- [ ] 3개 이상의 페이지
- [ ] 효과 3종 이상
- [ ] 변수와 조건문
- [ ] 타이머로 자동 페이지 이동
- [ ] `onClick` 이벤트로 콘솔 출력

완성했다면 `.ljm` 파일로 **Save**해서 공유하세요!
