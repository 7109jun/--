# JVideo (.jv) 파일 포맷 스펙 v1

JVideo Studio가 사용하는 `.jv` 파일 포맷의 공식 명세다. 텍스트 기반, 사람이 읽을 수 있고
(hex 위주), 확장 가능한 다단계 압축 파이프라인을 사용한다.

---

## 1. 설계 목표

- 텍스트 기반 (UTF-8, 실제로는 ASCII 서브셋만 사용)
- Git 친화적 (한 줄 텍스트, diff 가능)
- AI 친화적 (문법이 단순하고 결정론적)
- 오프라인/단일 파일 재생·편집 가능 (JVideo Studio 자체가 단일 HTML)
- 확장 가능한 압축 파이프라인 (새 압축기를 독립적으로 추가 가능)

## 2. 문자 집합 (Character Set)

파일 내부의 모든 데이터는 아래 문자만 사용한다. 그 외의 알파벳(G-Z 등)은
**절대 나타나지 않는다** — 구조를 나타내는 오프코드도 알파벳 대신 기호를 쓴다.

| 종류 | 문자 |
|---|---|
| 숫자 | `0123456789` |
| 영문(hex 전용) | `ABCDEF` |
| 구조 기호 | `~ , . ( ) & % # @ ? ^ * $ ! + -` |

hex 값은 항상 대문자다. 색상 값, 인덱스, 개수(count) 등 모든 수치는 16진수로 인코딩된다.

## 3. 전체 파일 구조

```
#1~<헤더>~<팔레트>~<프레임1>~<프레임2>~...~<프레임N>
```

`~` (FRAME_SEP) 로 구분되는 고정 순서의 섹션이다. 프레임 데이터 자체에는 `~`가
등장하지 않으므로 파일 전체를 `~` 기준으로 단순 split 하면 안전하게 파싱된다.

### 3.1 매직/버전

첫 섹션은 `#1` 고정 문자열이다 (버전 1). 파서는 이 값이 일치하지 않으면
"JV 파일 형식이 아니거나 버전이 다름" 오류를 낸다.

### 3.2 헤더

```
<width>,<height>,<fps>,<maxFrames>
```

4개 필드 모두 hex, `,`(LIST_SEP)로 구분. 예: `20,20,C,F0` = 32×32, 12fps, 최대 240프레임.

### 3.3 팔레트

```
<count>,<color1>,<color2>,...
```

- `count` : 팔레트에 등록된 색상 개수 (hex). **최대 7 (`PALETTE_MAX`)**.
- 각 `colorN` 은 `RRGGBB` 형식의 6자리 hex (예: `FF6B6B`).
- 팔레트 인덱스는 1부터 시작한다. **인덱스 0은 항상 "투명"으로 예약**되며
  팔레트 테이블에는 나타나지 않는다.

### 3.4 프레임

프레임 문자열은 첫 글자로 종류가 결정된다.

| 첫 글자 | 의미 |
|---|---|
| `#` | SAME — 직전 프레임과 완전히 동일 |
| `&` | FULL — 프레임 전체를 명시적으로 기술 |
| `%` | DIFF — 직전 프레임과의 차이만 기술 |

인코더는 프레임마다 FULL과 DIFF를 **둘 다 계산해서 더 짧은 쪽을 선택**한다
(SAME인 경우는 그 전에 이미 확정). 디코더는 첫 글자만 보고 바로 분기하므로
어느 쪽이 선택되었는지 신경 쓸 필요가 없다.

## 4. 압축 파이프라인

원본 → (SameFrame) → (Diff / ObjectMove / Delta) 또는 (RLE / TransparentSkip /
LineRepeat·BlockMerge / Dictionary) → 출력. 각 압축기는 독립된 함수/개념으로
분리되어 있어 새 압축기를 추가하기 쉽다 (§7 참고).

### 4.1 SameFrame

직전 프레임과 픽셀이 완전히 같으면 `#` 한 글자로 끝난다. 데이터 없음.

### 4.2 FULL 프레임 (`&`)

행(row) 단위로 인코딩하며, 3가지 하위 압축기가 함께 적용된다.

**RLE + TransparentSkip** — 한 행을 왼쪽부터 훑으며 동일 색이 연속되는 구간을
run으로 묶는다.

- 일반 색상 run: `<colorHex(2)>@<countHex>.`  예: `03@05.` = 색상 index 3이 5개 연속
- 투명 run(전용, 더 저렴): `?<countHex>.` 예: `?0A.` = 투명 10개 연속
  (TransparentSkip은 RLE의 특수 케이스이지만, 색상 hex를 아예 안 쓰므로 더 짧다)

한 행은 `width` 픽셀을 다 채울 때까지 run 토큰을 이어붙인 것이다. 별도의
종료 기호 없이, 디코더가 "이 행에서 몇 픽셀을 채웠는가"를 세면서 읽는다.

**LineRepeat / BlockMerge** — 연속된 행이 완전히 같으면, 그 행의 내용을 한 번만
쓰고 뒤에 `^<countHex>.` 를 붙여 "바로 다음 N개 행은 방금 행과 동일"이라고 표시한다.
(BlockMerge = 이 반복 개수가 2 이상인 경우, 여러 행을 하나의 블록으로 병합)

**Dictionary** — 프레임 안에서 어떤 행의 run 토큰 문자열이 **비연속적으로 2회 이상**
등장하면, 처음 등장할 때는 `*<runTokens>` (LIT+사전등록), 이후 재등장할 때는
`$<idxHex>.` (사전 참조)로 대체한다. 사전 인덱스는 등록 순서대로 0, 1, 2... 부여되며
별도로 인덱스를 저장하지 않고 등장 순서로 암묵적으로 결정된다 — 디코더도 같은
순서로 카운터를 올리며 사전 배열을 채운다.

| 마커 | 의미 |
|---|---|
| `=` | 그냥 이 행 그대로 (재사용 없음) |
| `*` | 이 행을 사전에 등록하면서 그대로 씀 (이후 재사용될 예정) |
| `$N.` | 사전 N번 항목을 그대로 복사 |
| `^N.` | 직전 행을 N번 반복 |

### 4.3 DIFF 프레임 (`%`)

```
%(<moveCount>.<move>;...)<changeCount>.<change>,<change>,...
```

**ObjectMove** — 이전 프레임과 비교해 "새로 나타난" 픽셀들의 경계상자를 구하고,
그 경계상자 전체가 이전 프레임의 다른 위치를 (dx, dy)만큼 평행이동한 것과 정확히
일치하는지 탐색한다(탐색 범위 ±32px, 경계상자는 프레임 면적의 50% 이하일 때만
시도 — 그 이상이면 단순 이동일 가능성이 낮아 탐색을 생략해 속도를 아낀다).
일치하면 픽셀을 나열하는 대신 이동 벡터 하나로 대체한다.

```
!<destX>,<destY>,<w>,<h>,<sdx>,<sdy>;
```

`sdx`/`sdy` 는 부호 있는 hex (`+`/`-` 접두 + hex, 예: `+8`, `-3`).

**Delta** — 이동으로 설명되지 않는 나머지 변경 픽셀들은 인덱스 오름차순으로 정렬한
뒤, 첫 항목은 절대 인덱스, 이후 항목은 직전 인덱스와의 차이(부호+hex)로 저장한다.

```
<absIdxHex>.<colorHex(2)>   ← 첫 항목
<sdelta>.<colorHex(2)>      ← 이후 항목들
```

## 5. 워크플로 예시

32×32, 프레임 3개(첫 프레임 FULL, 둘째 SAME, 셋째는 사각형이 이동)를 저장하면
대략 아래와 같은 모양이 된다 (실제 값은 내용에 따라 다름):

```
#1~20,20,C,F0~2,FF00FF,00AAFF~&*?18.^1.*?2.01@4.?12.^3.$0.^B.~#~%(1.!A,8,4,4,+8,+6;)0.
```

- `#1` : 버전
- `20,20,C,F0` : 32×32, 12fps, 최대 240프레임
- `2,FF00FF,00AAFF` : 팔레트 2색
- 첫 프레임(`&...`) : FULL, 사전(`*`)과 LineRepeat(`^`)이 적용됨
- 둘째 프레임(`#`) : SAME
- 셋째 프레임(`%...`) : DIFF, ObjectMove 1건, 남은 변경 0건

## 6. 알려진 한계 (v1)

- **ObjectMove는 프레임당 1개만 탐지**한다. 여러 물체가 동시에 움직이면 하나만
  이동으로 처리되고 나머지는 픽셀 단위 Delta로 저장된다.
- **Dictionary는 프레임 내부에서만** 동작한다. 여러 프레임에 걸쳐 반복되는 배경
  패턴을 공유하는 프레임 간(cross-frame) 사전은 아직 없다.
- 팔레트는 **최대 7색**으로 고정되어 있어(`PALETTE_MAX`), 색이 다양한 사진/영상
  소스(MP4 가져오기)는 k-means 양자화로 강하게 근사된다.
- Delta로 남는 개별 변경 픽셀들은 색상별로 다시 run-length 압축되지 않는다
  (인접한 같은 색 변경 픽셀이 많아도 하나씩 나열됨) — 향후 확장 여지로 남겨둠.

## 7. 확장 방법

새 압축기를 추가하려면:

1. `encode*(...)` / `decode*(...)` 함수 쌍을 순수 함수로 작성한다 (부수효과 없이
   문자열 ↔ 픽셀 배열 변환만 담당).
2. 사용할 opcode 기호가 §2 문자 집합 안에 있고 기존 opcode와 겹치지 않는지 확인한다.
3. `encodeFrame` / `decodeFrame` 분기(SAME/FULL/DIFF)에 새 프레임 타입을 추가하거나,
   기존 FULL/DIFF 내부의 하위 압축 단계에 끼워 넣는다.
4. 반드시 인코드→디코드 왕복 결과가 원본과 완전히 같은지 검증한다.

## 8. 레이어와의 관계 (중요)

JVideo Studio의 **레이어는 편집 세션에만 존재하는 개념이며 `.jv` 파일에는 전혀
기록되지 않는다.** 저장/내보내기 시점에는 항상 보이는 레이어를 아래→위 순서로
합성(flatten)한 단일 픽셀 배열만 저장된다. 즉 이 스펙 문서의 "프레임"은 항상
합성이 끝난 최종 결과 1장을 의미하며, 레이어 개수·순서·이름·가시성 같은 편집
정보는 파일을 열 때 복원되지 않는다 (다시 열면 단일 레이어로 로드된다).
