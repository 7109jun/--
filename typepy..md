# typepy
<img width="814" height="860" alt="스크린샷 2026-08-08 223925" src="https://github.com/user-attachments/assets/1141a10f-00dd-41a2-a62e-f6bbeb1c417d" />


## typepy 학습 가이드

이 문서는 C로 구현된 Python 스마트 타입 변환 모듈 typepy를 이해하기 위한 학습 자료입니다.
typepy가 왜 만들어졌는지, 어떻게 동작하는지, 그리고 직접 사용하면서 무엇을 주의해야 하는지를 순서대로 설명합니다.

---

## 1. typepy란 무엇인가

typepy는 Python의 동적 타이핑이 만드는 애매함을 C 레벨에서 정리해주는 단일 파일 모듈입니다.

Python은 매우 유연하지만, 그 유연함 때문에 다음과 같은 문제가 생깁니다.

- 문자열 "123"과 숫자 123이 섞여 들어와도 늦게야 발견된다
- True가 1로 취급되어 계산 결과가 미묘하게 틀어진다
- 숫자로 바꾸려다 실패하면 어디서 예외가 터질지 예측하기 어렵다

typepy는 이런 값을 받아서 스스로 판단하고, 가능하면 숫자로 바꾸고, 불가능하면 안전하게 문자열로 떨어뜨립니다.
이 모든 과정이 C 코드 안에서完결되기 때문에 Python 래퍼가 필요 없습니다.

---

## 2. 설계 철학

typepy의 동작 원칙은 다음 세 가지입니다.

### 2.1 판단과 변환을 분리하지 않는다

많은 도구가 "타입 확인"과 "변환"을 따로 제공합니다.
typepy는 이 둘을 하나로 합쳤습니다. smart_convert 하나만 호출하면 됩니다.

### 2.2 절대 중간에 멈추지 않는다

잘못된 값이 들어와도 예외를 던지고 종료하지 않습니다.
숫자로 바꿀 수 없으면 문자열로 반환합니다.
데이터 파이프라인에서 하나의 불량 값 때문에 전체가 중단되는 것을 막기 위한 설계입니다.

### 2.3 단, 진짜 치명적인 오류는 숨기지 않는다

메모리 부족 같은 상황은 "변환 실패"가 아닙니다.
이런 경우는 예외를 그대로 상위에게 전달합니다.
정상적인 변환 실패와 시스템 차원의 실패를 명확히 구분하는 것이 typepy 안정성의 핵심입니다.

---

## 3. 빌드하기

typepy는 C 파일 하나로 이루어져 있습니다. 아래 명령으로 빌드합니다.

    gcc -O3 -shared -fPIC \
        -o typepy$(python3-config --extension-suffix) \
        typepy.c \
        $(python3-config --cflags --ldflags) -lm

여기서 -lm은 수학 함수를 링크하기 위해 필요합니다.

빌드가 성공하면 Python에서 바로 import 할 수 있는 공유 라이브러리가 만들어집니다.

---

## 4. 기본 사용법

사용법은 매우 단순합니다.

    import typepy

    typepy.smart_convert("123")
    # 결과: 123

    typepy.smart_convert("3.14")
    # 결과: 3.14

    typepy.smart_convert("hello")
    # 결과: "hello"

    typepy.smart_convert(None)
    # 결과: ""

리스트 전체를 변환하려면 smart_convert_list를 사용합니다.

    typepy.smart_convert_list([1, "2", "3.5", "hello", None])
    # 결과: [1, 2, 3.5, "hello", ""]

---

## 5. 변환 규칙 총정리

smart_convert가 입력을 어떻게 처리하는지 표로 정리했습니다.

| 입력 타입 | 변환 결과 | 설명 |
|-----------|-----------|------|
| None | 빈 문자열 "" | None은 빈 문자열로 정규화 |
| bool | int | True는 1, False는 0 |
| int | int | 그대로 반환 |
| float | float | 그대로 반환 |
| 숫자 형태 문자열 | int 또는 float | "123"은 int, "3.14"는 float |
| 일반 문자열 | 문자열 | 앞뒤 공백만 제거 후 반환 |
| 그 외 객체 | int → float → str 순서 시도 | 전부 실패하면 str |

문자열 숫자 변환에서 기억해야 할 세부 규칙은 다음과 같습니다.

- "123", "-123", "+123"은 모두 정수로 변환됩니다
- "3.14", "-3.14", "1e10"은 실수로 변환됩니다
- int64 범위를 넘는 큰 정수 문자열도 Python의 임의 정밀도 int로 변환됩니다
- 공백은 ASCII 스페이스와 탭만 제거합니다. Unicode 공백은 제거하지 않습니다

---

## 6. 내부 동작 흐름

smart_convert 내부에서는 다음 순서로 판단이 이루어집니다.

    입력 값 도착
        |
        +-- None인가? -> 빈 문자열 반환
        |
        +-- int, float, str 그 자체인가? -> 그대로 반환
        |
        +-- bool인가? -> 0 또는 1 반환
        |
        +-- 문자열인가?
        |       |
        |       +-- 공백 제거
        |       +-- 정수 형태인가? -> int 변환 시도
        |       +-- 실수 형태인가? -> float 변환 시도
        |       +-- 둘 다 아니면 -> 문자열 반환
        |
        +-- 그 외 객체
                |
                +-- int 변환 시도
                +-- float 변환 시도
                +-- str 강제 변환

이 흐름에서 중요한 점은 모든 분기가 유효한 결과를 반환한다는 것입니다.
중간에 멈추는 경우가 없도록 설계되어 있습니다.

---

## 7. 에러 처리 정책 이해하기

typepy를 제대로 이해하려면 에러 처리 정책을 알아야 합니다.

### 7.1 정상적인 변환 실패

숫자가 아닌 문자열, 범위를 벗어난 값 등은 정상적인 변환 실패로 취급합니다.
이 경우 내부적으로 PyErr를 정리하고 다음 변환을 시도하거나 문자열로 폴백합니다.
사용자 입장에서는 예외가 보이지 않습니다.

### 7.2 치명적인 실패

메모리 할당 실패는 변환 실패가 아닙니다.
이 경우 PyErr_NoMemory로 설정된 예외를 그대로 전파합니다.
코드의 여러 곳에서 PyErr_Occurred를 검사하는 이유가 바로 이것입니다.

    정상 실패 -> PyErr 없음 -> 다음 변환 시도
    치명 실패 -> PyErr 유지 -> 즉시 상위로 전달

이 규칙을 지키지 않으면 메모리 부족 상황에서 잘못된 결과가 반환되는 치명적인 버그가 생길 수 있습니다.

---

## 8. 코드로 보는 핵심 로직

### 8.1 정수 파싱

문자열이 정수 형태인지 확인한 뒤, PyLong_FromString으로 변환합니다.
이 함수는 임의 정밀도를 지원하므로 매우 큰 숫자도 문제없습니다.

    _str_is_integer로 형태 확인
    null-terminated 복사본 생성
    PyLong_FromString으로 변환
    end 포인터로 전체 소비 여부 확인

여기서 주의할 점은 버퍼를 해제하기 전에 end 포인터를 먼저 확인해야 한다는 것입니다.
순서가 바뀌면 이미 해제된 메모리를 참조하는 문제가 생깁니다.

### 8.2 실수 파싱

실수 파싱은 한 번만 수행됩니다.
strtod로 파싱하면서 동시에 유효성을 검사하고, 성공하면 바로 PyFloat를 만듭니다.

유효성의 조건은 세 가지입니다.

- 최소 한 글자 이상 소비했을 것
- 문자열 끝까지 모두 소비했을 것
- ERANGE가 발생하지 않았을 것

### 8.3 문자열 폴백

최종 폴백은 PyObject_Str를 사용하고, 실패하면 PyObject_Repr를 시도합니다.
둘 다 실패하면 마지막 수단으로 대체 문자열을 반환합니다.
이 과정에서도 메모리 부족 예외는 절대 삼키지 않습니다.

---

## 9. 흔한 오해와 주의사항

### 9.1 리스트를 smart_convert에 넣으면?

리스트를 smart_convert에 직접 넣으면 요소별로 변환되지 않습니다.
리스트라는 객체 자체를 문자열로 폴백합니다.

    typepy.smart_convert([1, "2"])
    # 결과: "[1, '2']"

요소별 변환이 필요하면 반드시 smart_convert_list를 사용해야 합니다.

### 9.2 NaN과 Inf 처리

문자열 "nan"이나 "inf"는 strtod가 인식할 수 있으므로 float로 변환됩니다.
이것은 strtod의 표준 동작을 따른 것입니다.
만약 수치 연산에서 NaN을 차단하고 싶다면 typepy의 변환 결과를 받은 뒤 별도로 검증해야 합니다.

### 9.3 bool은 int의 하위 타입이다

Python에서 bool은 int의 하위 클래스입니다.
typepy는 bool을 명시적으로 검사해서 0 또는 1로 변환합니다.
이 검사가 int 체크보다 뒤에 위치하는 이유는, 정확한 int 타입을 먼저 처리하고 bool은 별도로 다루기 위해서입니다.

---

## 10. 연습 문제

다음 문제를 직접 풀어보면서 이해도를 확인해보세요.

1. smart_convert("  42  ")의 결과는 무엇인가? 왜 그런가?

2. smart_convert("123456789012345678901234567890")가 문자열로 폴백되지 않는 이유를 설명하라.

3. 메모리 부족 상황에서 typepy가 예외를 던지는 것이 왜 올바른 설계인지 설명하라.

4. smart_convert와 smart_convert_list의 차이점을 각각의 사용 예와 함께 설명하라.

5. 문자열 "1e400"은 어떤 결과가 나오는가? ERANGE와 관련지어 설명하라.

---

## 11. 정리

typepy를 한 문장으로 요약하면 다음과 같습니다.

판단은 C가 하고, 변환도 C가 하고, 실패하면 문자열로, 단 메모리 부족은 예외로.

이 규칙만 기억하면 typepy의 모든 동작을 예측할 수 있습니다.
유연함이 필요한 곳에서는 Python을 쓰고, 확실함이 필요한 경계에서는 typepy로 값을 정규화하는 패턴을 권장합니다.
