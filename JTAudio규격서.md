# JTAudio (.jta) v1.4 — 압축률 강화판 규격서 

> JTAudio는 MP3의 대체제가 아닙니다. 바이너리를 쓸 수 없는 환경(Markdown, TXT, JSON, YAML, XML,
> 이메일, AI 프롬프트, GitHub, 채팅)에서 오디오를 저장·전송하기 위한 UTF-8 텍스트 오디오
> 규격입니다. v1.4는 **압축률에만 집중**한 업그레이드입니다 — 문법은 지난 버전들과 100% 호환되고,
> 새 기법은 전부 "이미 잘 알려진, 이상하지 않은" 방식(반복 감지)의 자연스러운 확장입니다.

---

## 0. 핵심 결론부터 (정직하게)

이번 버전의 목표는 "**base64로 인코딩한 MP3보다 작게**"였습니다. 실측 결과:

| 콘텐츠 성격 | 6bit | 8bit | 10bit | 12bit | 16bit |
|---|---|---|---|---|---|
| **반복 구조가 있는 곡**(인트로+벌스+코러스×2, 실측) | **19%** | **31%** | **46%** | **57%** | 106% |
| **반복이 거의 없는 광대역 신호**(최악의 경우, 실측) | 135% | 205% | 358% | 486% | 더 커짐 |

(퍼센트 = JTAudio 파일 크기 ÷ base64(MP3 128kbps) 크기 × 100. 100% 미만이면 JTAudio가 더 작다는 뜻)

**실제 노래 대부분에는 벌스/코러스/루프 같은 반복 구조가 있으므로, "매우 적음"부터 "높음"까지
4단계 품질에서는 base64(MP3)보다 확실히 작습니다** — 코러스가 2번 이상 반복되는 곡이면 12bit
("높음", 손실률 약 0.05%)로도 base64(MP3)보다 43% 더 작았습니다. 다만 **"매우 높음"/"무손실급"
(16bit 이상)은 MP3와 비슷하거나 커질 수 있고, 반복이 전혀 없는 콘텐츠(백색소음에 가까운 신호)는
어떤 품질 단계에서도 이기지 못했습니다.** 이는 추측이 아니라 실측이며, 이유는 1절에서 설명합니다.

---

## 1. 왜 "모든 경우에 항상" 이기지는 못하는가 (솔직한 설명)

MP3는 사람의 청각 특성(심리음향 모델)을 이용해 "들리지 않는 정보를 아예 버리는" 방식으로 작습니다.
JTAudio는 여전히 **PCM(파형) 자체를 저장**하고, 그 안의 **반복·중복만** 찾아서 줄이는 방식입니다.
즉 두 기술은 "무엇을 줄이는가"가 다릅니다:

- MP3: 사람이 못 듣는 주파수 성분을 통째로 삭제 → 작아짐 (손실이 정보 삭제에서 옴)
- JTAudio: 파형 안의 반복·중복 패턴만 압축 → 반복이 많을수록 작아짐 (손실은 양자화에서만 옴)

그래서 **반복이 많은 콘텐츠**(노래의 코러스, 루프, 무음 구간)에서는 JTAudio가 유리하고,
**반복이 없는 콘텐츠**(순수 노이즈, 매번 다른 소리)에서는 MP3의 삭제 전략을 이길 방법이
텍스트 기반 무손실급 접근으로는 없습니다. 이는 JTAudio가 "MP3의 대체제가 아니다"라고 처음부터
밝힌 것과 정확히 같은 이유입니다 — 이번 업그레이드로도 그 목표 자체는 바꾸지 않았습니다.

**결론적 권장사항**: base64(MP3)보다 작은 파일이 필요하다면 품질을 "높음"(12bit) 이하로
선택하세요. "무손실급"은 여전히 제공되지만(용도: 로컬 Git 저장처럼 파일 크기가 중요하지 않은 경우),
텍스트 전송 크기가 중요하다면 권장하지 않습니다.

---

## 2. 이번 버전에서 무엇을 추가했나: "반복 감지"를 더 똑똑하게

지난 버전까지의 반복 감지:
- **RLE**: 숫자 하나가 반복되면 압축 (`O`가 5번 반복 → `O+5`와 같은 원리)
- **청크 단위 중복**: 청크 전체가 이전 청크와 완전히 같으면 통째로 재사용

이번 버전이 추가한 것: **길이 제한 없는 반복 감지(LZ 방식)**. 원리는 똑같이 단순합니다 —
"어떤 덩어리가 예전에 나온 적이 있으면, 다시 쓰지 말고 그냥 뒤로 몇 글자, 몇 글자만큼
가리키는 짧은 표시만 남긴다"는 것뿐입니다. 다만 이번엔:

1. **글자 하나가 아니라 임의 길이의 덩어리**를 통째로 찾습니다(`ABCABCABC` → `ABC`+"뒤로 3칸,
9글자 복사"). 이건 zip/gzip이 쓰는 것과 똑같은, 아주 잘 알려진 방식입니다.
2. **한 청크 안이 아니라 노래 전체**에서 찾습니다. v1.3까지는 약 46ms(청크 하나) 안에서만
반복을 찾았는데, 실제 노래의 코러스 반복은 수 초~수십 초 뒤에 다시 나옵니다. v1.4는 파일
전체를 뒤져서 "이 부분, 17초 전 코러스랑 똑같네? 그럼 그냥 그쪽을 가리키자"를 할 수 있습니다.

### 2.1 새 codec: `JTA-DPCM2-RLE82-LZG`

| codec | 반복 탐색 범위 | 도입 버전 |
|---|---|---|
| `JTA-DPCM2-RLE82` | 없음(델타+RLE만) | v1.2 |
| `JTA-DPCM2-RLE82-LZ` | 청크 안(~46ms) | v1.3 |
| `JTA-DPCM2-RLE82-LZG` (신규) | **파일 전체** | v1.4 |

새 토큰 형식(STREAM 안, 예약 문자 1개 추가): `` `<거리><길이> `` — 백틱(`` ` ``)은 이미 v1.2부터
JSON/YAML 안전을 위해 알파벳에서 제외돼 있던 문자라 **새로 예약해도 기존 82문자 알파벳이
줄어들지 않습니다.**

### 2.2 어떻게 노래 전체를 빠르게 뒤지나 (성능)

"파일 전체를 매번 처음부터 끝까지 비교"하면 노래가 길어질수록 기하급수적으로 느려집니다.
그래서 실제 zip/gzip처럼 **해시 인덱스**를 씁니다: 지금까지 나온 모든 4글자 조각의 위치를
표에 기록해 두고, 새 위치를 처리할 때 "이 4글자로 시작하는 데가 예전에 있었나?"만 표에서
바로 찾습니다(전체를 훑지 않음). 탐색 범위는 최근 후보 24~64개로 제한해 속도와 압축률 사이의
균형을 잡았습니다. 실측: 20초 분량 합성곡 인코딩 약 0.3~1초, 디코딩 약 0.1초(Node.js 기준).

---

## 3. 실측 결과 상세

### 3.1 테스트 신호 A — 반복 구조가 있는 합성곡 (22050Hz, 스테레오, 20초)

인트로(2초) → 벌스(4초) → 코러스(4초) → 벌스(4초, 1번과 동일) → 코러스(4초, 1번과 동일) →
아웃트로(2초) 구조로, 실제 대중음악의 전형적인 곡 구조를 흉내냈습니다.

| bit_depth | JTAudio 크기 | base64(MP3@128kbps) 대비 |
|---|---|---|
| 6 (매우 적음) | 67,240 B | **16%** (약 6.3배 작음) |
| 8 (적음) | 121,943 B | **29%** (약 3.5배 작음) |
| 10 (중간) | 189,985 B | **45%** (약 2.2배 작음) |
| 12 (높음, 권장) | 235,944 B | **55%** (약 1.8배 작음) |
| 16 (매우 높음) | 449,030 B | 105% (거의 같음) |

### 3.2 테스트 신호 B — 반복이 거의 없는 광대역 신호 (최악의 경우, 22050Hz, 스테레오, 6초)

13개의 서로 다른 배음 + 시간에 따라 변하는 미세한 주파수 변조 + 노이즈 바닥 — 반복 패턴이
의도적으로 거의 없도록 구성했습니다.

| bit_depth | JTAudio 크기 | base64(MP3@128kbps) 대비 |
|---|---|---|
| 6 | 172,902 B | 135% |
| 8 | 262,169 B | 205% |
| 12 | 457,807 B | 358% |
| 16 | 622,331 B | 486% |

**주의**: 테스트 신호 A는 벌스/코러스가 수학적으로 완전히 동일하게 반복되도록 만든
"이상적인" 경우입니다. 실제 녹음된 노래는 같은 코러스를 불러도 미세한 타이밍·표현 차이가
있으므로, 실제 곡에서의 이득은 이 수치보다 다소 낮을 수 있습니다(그래도 방향성은 유효합니다 —
반복 구조가 있으면 있을수록 유리).

---

## 4. GitHub 독자를 위한 실습 가이드

### 4.1 업로드 없이 바로 체험하기 — 테스트 노래 직접 생성해보기

리포지토리를 막 받은 사람이 mp3 파일이 없어도 바로 테스트할 수 있도록, 첨부된
`jtaudio_v1_4.html`에는 **"🎼 테스트 노래 생성"** 버튼이 있습니다. 클릭하면 브라우저의
Web Audio API 오실레이터로 인트로+벌스+코러스 구조를 가진 짧은 곡을 그 자리에서 합성해
바로 인코딩/디코딩/재생을 시도해볼 수 있습니다. 외부 파일이나 서버 없이, 완전히 로컬에서
동작합니다.

**순서**:
1. `jtaudio_v1_4.html`을 브라우저로 엽니다.
2. "🎼 테스트 노래 생성" 클릭 → 20초 분량의 합성곡이 만들어지고 자동으로 인코딩 대상으로 등록됩니다.
3. 품질(비트 해상도)을 골라 "⚙️ .jta로 인코딩" 클릭 → 압축률·base64(MP3) 대비 비율이 바로 표시됩니다.
4. "⬇ .jta 다운로드"로 파일을 저장하거나, "📋 텍스트 복사"로 클립보드에 복사해 이 문서처럼
GitHub README나 이슈에 직접 붙여넣어볼 수 있습니다.

### 4.2 실제 노래를 JTAudio로 바꾸는 과정

1. **원본 준비**: mp3 또는 wav 파일을 준비합니다(저작권에 문제 없는 파일을 사용하세요 — 예:
   프리사운드(freesound.org)나 직접 녹음한 파일, 또는 위 4.1의 테스트 노래 생성 기능).
2. **업로드**: `jtaudio_v1_4.html`의 "MP3 / WAV 파일 업로드" 영역에 드래그하거나 클릭해 파일을
   선택합니다. 브라우저가 자동으로 디코딩해 샘플레이트·채널·길이를 보여줍니다.
3. **품질 선택**: 목적에 맞게 고릅니다.
   - GitHub README, 이메일, 채팅 등 **텍스트 전송이 목적**이면 → "매우 적음"~"높음"(6~12bit) 권장
     (base64(MP3)보다 확실히 작음, 3.1절 실측 참고)
   - **로컬 보관/최대 보존**이 목적이면 → "매우 높음"/"무손실급"(16~20bit), 파일 크기는
     base64(MP3)보다 커질 수 있음을 감안
4. **인코딩**: "⚙️ .jta로 인코딩" 클릭. 내부적으로 discrete/mid_side 스테레오 모드를 둘 다
   시도해 더 작은 쪽을 자동 선택하고, 채널·청크마다 1차/2차 델타, RLE, 파일 전체 반복 탐색(LZG)을
   모두 적용한 뒤 결과를 보여줍니다.
5. **확인**: 압축비, 추정 손실률(RMS), base64(MP3) 대비 비율이 카드에 표시됩니다. "전체 보기"로
   실제 텍스트를 확인하거나 "복사"로 클립보드에 담을 수 있습니다.
6. **다운로드 또는 붙여넣기**: `.jta` 파일로 저장하거나, 텍스트를 그대로 복사해 GitHub 이슈,
   커밋 메시지, README, 이메일 본문 등에 붙여넣습니다.
7. **되돌리기(디코딩)**: 나중에 재생하려면 같은 페이지의 "디코딩 & 재생" 카드에 `.jta` 파일을
   업로드하거나, 텍스트를 그대로 붙여넣기 칸에 붙여넣고 "붙여넣은 텍스트 재생"을 누르면 됩니다 —
   파일이 없어도, 텍스트만 있으면 재생됩니다.

### 4.3 결과를 검증하는 법

다운로드한(또는 붙여넣은) `.jta` 파일의 `[END]` 블록에 있는 `checksum=CRC32:...` 값은
`[STREAM]` 블록 전체로부터 계산됩니다. 디코더는 재생 전 이 값을 다시 계산해 비교하고, 화면에
"체크섬: ✓ 유효" 또는 "✗ 손상됨" 배지로 보여줍니다 — 파일을 복사/붙여넣기 하다 일부가
잘렸는지 여부를 바로 알 수 있습니다.

---

## 5. 하위 호환성

- v1.0~v1.3으로 만든 모든 `.jta` 파일은 v1.4 디코더로 **완전히 정상 재생**됩니다(회귀 테스트 확인).
- v1.4가 새 `codec=JTA-DPCM2-RLE82-LZG`를 실제로 사용한 파일은, 이 codec을 모르는 구버전
  디코더에서는 정상 재생을 보장하지 않습니다 — 지난 버전들과 동일한 이유입니다: 새 기능은
  반드시 `codec` 필드로 선언되고, 이를 검증하는 디코더는 조용히 깨지는 대신 명확한 오류를 냅니다.
- 컨테이너 구조(`[HEADER]/[META]/[STREAM]/[END]`), 알파벳, RLE·2차델타·mid/side 문법은
  전부 이전 버전과 100% 동일합니다. 이번 버전은 STREAM 안에 새 예약 문자(`` ` ``) 하나를
  추가로 사용하는 것 외에는 아무 문법도 바꾸지 않았습니다.

---

## 6. 성능·한계에 대한 정직한 메모

- 파일 전체를 뒤지는 만큼, v1.3(청크 단위)보다 인코딩 시간이 더 걸립니다(실측: 20초 곡 기준
  약 0.3~1초, 품질이 높을수록(데이터가 많을수록) 더 걸림). 브라우저에서 수 분 분량의 긴
  음원을 다루면 체감 지연이 생길 수 있습니다 — 이 도구가 원래 지향하는 "짧은 클립" 용도에서는
  문제되지 않는 수준입니다.
- 탐색 범위(해시 체인 길이, 검색 윈도)를 늘리면 압축률이 조금 더 오르지만 인코딩 시간도
  늘어나는 트레이드오프가 있습니다. 현재 설정(체인 64개, 윈도 최대 1MB 상당)은 이 도구의
  목표(짧은 클립, 브라우저 실행)에 맞춘 균형점이며, 필요하면 조정 가능합니다.
- 0절의 "매우 높음/무손실급이 base64(MP3)보다 커질 수 있다"는 한계는 이번 버전에서도 해결하지
  못했고, 앞서 설명했듯 PCM 도메인 반복 압축 방식의 근본적인 한계입니다 — 이를 해결하려면
  심리음향 모델(주파수 대역별 인지 손실 압축)이 필요한데, 이는 JTAudio를 "MP3처럼" 만드는
  것이라 애초의 설계 목표(MP3의 대체제가 아님)와 충돌합니다. 그래서 이번 버전에도 추가하지
  않았습니다.
