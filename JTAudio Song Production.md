# JTAudio로 노래 잘 만드는 법 — 처음부터 끝까지 튜토리얼

이 문서는 규격서(`JTAudio_Spec_v1.2.md`)의 "실습판"입니다. 규격서가 "무엇이 어떻게 동작하는가"를
다룬다면, 이 문서는 **"어떻게 하면 실수 없이 잘 만드는가"**를 다룹니다. 특히:

- 에러가 왜 이렇게 많이 나는지 (사실 종류는 몇 개 안 됩니다 — 하나씩 짚어드립니다)
- 초보자가 제일 많이 하는 실수 (특히 여는 태그/닫는 태그 관련)
- 처음부터 끝까지, 곡 하나를 만들어서 GitHub에 올리기까지의 전 과정

을 하나부터 끝까지 순서대로 설명합니다.

---

## 목차

1. [5분 안에 첫 곡 만들기](#1-5분-안에-첫-곡-만들기)
2. [.jta 파일의 구조 제대로 이해하기 (태그가 전부입니다)](#2-jta-파일의-구조-제대로-이해하기-태그가-전부입니다)
3. [좋은 압축률을 위한 선택 가이드](#3-좋은-압축률을-위한-선택-가이드)
4. [에러 코드 총정리 — 왜 뜨는지, 어떻게 고치는지](#4-에러-코드-총정리--왜-뜨는지-어떻게-고치는지)
5. [초보자가 제일 많이 하는 실수 TOP 10](#5-초보자가-제일-많이-하는-실수-top-10)
6. [체크섬으로 내 파일이 멀쩡한지 스스로 확인하기](#6-체크섬으로-내-파일이-멀쩡한지-스스로-확인하기)
7. [GitHub에 올리기 전 체크리스트](#7-github에-올리기-전-체크리스트)
8. [자주 묻는 질문 (FAQ)](#8-자주-묻는-질문-faq)

---

## 1. 5분 안에 첫 곡 만들기

1. `jtaudio_v1_2.html` 파일을 더블클릭해서 브라우저로 엽니다(인터넷 없이도 동작합니다. MP3
   다운로드 기능만 인터넷이 필요합니다).
2. **"1. 인코딩"** 카드의 점선 박스에 mp3 또는 wav 파일을 끌어다 놓거나 클릭해서 선택합니다.
   - 저작권 걱정 없는 파일을 쓰세요: 직접 녹음한 파일, 프리사운드(freesound.org)의 CC 라이선스
     효과음, 또는 짧은 보이스 메모 등을 추천합니다.
3. 파일 정보(샘플레이트·채널·길이)가 뜨면 정상적으로 읽힌 것입니다.
4. **품질**을 고릅니다. 잘 모르겠다면 기본값인 "높음 · 12bit"를 그대로 두세요 — 손실이 거의
   없으면서도 파일이 과하게 커지지 않는 균형점입니다.
5. **"⚙️ .jta 로 인코딩"** 버튼을 누릅니다. 압축비, 손실률, 결과 텍스트 미리보기가 나타납니다.
6. **"⬇ .jta 다운로드"**로 파일을 저장하거나, **"📋 텍스트 복사"**로 클립보드에 담아 GitHub
   README나 이슈, 커밋 메시지 등에 그대로 붙여넣습니다.
7. 나중에 다시 재생하려면: **"2. 디코딩 & 재생"** 카드에 그 `.jta` 파일을 업로드하거나, 텍스트를
   붙여넣기 칸에 붙여넣고 **"📋 붙여넣은 텍스트 재생"**을 누르면 됩니다.
8. 재생이 잘 된다면 **"⬇ WAV로 다운로드"** 또는 **"⬇ MP3로 다운로드"**로 일반적인 오디오 파일
   형식으로도 저장할 수 있습니다.

여기까지가 전체 과정입니다. 이제부터는 "왜 이게 이렇게 동작하는지"와 "뭘 하면 안 되는지"를
자세히 다룹니다.

---

## 2. .jta 파일의 구조 제대로 이해하기 (태그가 전부입니다)

`.jta` 파일은 아래처럼 **여는 태그와 닫는 태그가 정확히 한 쌍씩** 있는 4개의 블록으로 되어
있습니다. HTML이나 XML을 다뤄본 적이 있다면 익숙한 개념입니다.

```
#JTA1.0                 ← 반드시 파일의 첫 줄, 정확히 이 8글자
[HEADER]                ← 여는 태그
title=hello
sample_rate=44100
...
[/HEADER]               ← 닫는 태그 (슬래시 위치, 철자 정확히!)

[META]
title=hello
[/META]

[STREAM]
@CHUNK 0 samples=2048 time=0.000-0.046
0w4u4s4m4g4a4y3o3e3y2...
[/STREAM]

[END]
chunks=65
checksum=CRC32:9F3E21A0
[/END]
```

### 태그 관련 절대 규칙

| 규칙 | 어기면 생기는 일 |
|---|---|
| 여는 태그와 닫는 태그는 **반드시 쌍으로** 존재해야 함 | "블록을 찾을 수 없습니다" 오류 |
| 닫는 태그는 `[/이름]` 형태(슬래시 `/` 필수) | 슬래시를 빼먹으면 "닫는 태그"가 아니라 그냥 또 다른 열린 태그로 인식되어 블록 검색이 꼬임 |
| 태그 이름은 **대소문자까지 정확히 일치** (`[HEADER]` ≠ `[Header]`) | 소문자로 치면 못 찾음 |
| 첫 줄은 정확히 `#JTA1.0` (공백·BOM 없이) | "지원하지 않는 JTAudio 버전입니다" 오류 |
| 4개 블록의 순서는 HEADER → META → STREAM → END | 순서를 바꾸면 파싱 자체는 되지만(태그 이름으로 찾으므로), STREAM 안에 우연히 `[HEADER]`라는 글자가 들어갈 일이 없으므로 순서를 지키는 것이 안전합니다 |

**왜 이렇게 엄격한가?** 이 파서는 "정확히 일치하는 문자열을 찾는" 아주 단순한 방식으로
동작합니다. 복잡한 XML/JSON 파서처럼 태그 짝을 스스로 추론해주지 않습니다 — 대신 그만큼
가볍고, 사람이 읽고 고치기 쉽고, AI가 생성하기도 쉽습니다. 그 대가로 **태그 철자와 짝은
사람이 정확히 맞춰야** 합니다.

---

## 3. 좋은 압축률을 위한 선택 가이드

| 상황 | 추천 설정 |
|---|---|
| GitHub README, 이메일, 채팅에 붙여넣을 목적 | 품질 6~12bit(매우 적음~높음), 청크 2048 |
| 로컬 보관용, 최대한 원본에 가깝게 | 품질 16~20bit(매우 높음/무손실급) |
| 스테레오 음원이고 좌우가 비슷한 소리(보컬+반주 등) | 자동으로 mid_side가 선택되는 경우가 많음 — 따로 설정할 필요 없음 |
| 무음/반복 구간이 많은 음원(루프, 짧은 효과음) | 어떤 품질에서도 파일이 특히 작게 나옵니다 |
| 완전히 새로운 소리가 계속 나오는 음원(노이즈에 가까운 것) | 압축이 잘 안 될 수 있습니다 — 정상입니다, 반복이 없으면 압축할 것도 없습니다 |

인코딩 후 화면에 뜨는 **"추정 손실률 (RMS)"**가 2% 이하면 사람 귀로 거의 구분이 안 되는
수준입니다. 5% 이상이면 실제로 들어보고 괜찮은지 확인하세요.

---

## 4. 에러 코드 총정리 — 왜 뜨는지, 어떻게 고치는지

이 도구가 낼 수 있는 오류 메시지는 정해져 있습니다. 아래 표에서 정확히 뜬 문구를 찾아
원인과 해결법을 확인하세요.

### 4.1 `지원하지 않는 JTAudio 버전입니다 (알 수 없는 매직 문자열)`

- **원인**: 파일(또는 붙여넣은 텍스트)의 첫 줄이 `#JTA1.0`이 아님.
- **자주 있는 실수**:
  - 첫 줄 앞에 빈 줄이나 공백이 있음
  - 워드프로세서나 메모장이 파일 맨 앞에 보이지 않는 BOM(Byte Order Mark) 문자를 추가함
  - 애초에 `.jta` 파일이 아닌 다른 텍스트를 붙여넣음
- **해결**: 첫 줄이 정확히 `#JTA1.0`으로 시작하는지 확인. 메모장 대신 VS Code 같은 코드
  에디터로 열어서 확인하면 숨은 문자를 발견하기 쉽습니다.

### 4.2 `.jta 파일 구조가 올바르지 않습니다: [HEADER] 블록을 찾을 수 없습니다` (META/STREAM/END도 동일)

- **원인**: 해당 이름의 여는 태그 또는 닫는 태그가 없음(2절 참고).
- **자주 있는 실수**:
  - 텍스트를 복사할 때 끝부분이 잘려서 `[/END]`가 통째로 빠짐(긴 파일을 채팅창 등에서 복사할 때
    흔함 — 스크롤이 끝까지 안 됐거나 글자 수 제한에 걸림)
  - 태그를 손으로 고치다 오타(`[HEADR]`, `[/Header]`, `[ HEADER ]`처럼 공백이 들어감)
- **해결**: 파일 맨 끝에 `[/END]`가 있는지, 각 블록의 여는/닫는 태그 철자가 정확한지 확인.
  전체 복사 시 "전체 보기" 버튼으로 텍스트를 다 펼친 뒤 복사하세요(미리보기 상태로 복사하면
  일부만 복사됩니다!).

### 4.3 `지원하지 않는 codec입니다: "..."`  /  `지원하지 않는 stereo_mode입니다: "..."`

- **원인**: `[HEADER]`의 `codec=` 또는 `stereo_mode=` 값이 이 디코더가 아는 값이 아님.
- **자주 있는 실수**:
  - 값의 대소문자를 바꿈(`jta-dpcm2-rle82`처럼 소문자로 — 실제 값은 `JTA-DPCM2-RLE82`처럼
    대문자여야 함)
  - 다른(더 새로운) 버전의 인코더로 만든 파일을 예전 디코더로 열려고 함
  - 오타(`JTA-DPCM2-RLE28`처럼 숫자 순서가 바뀜)
- **해결**: 값의 철자·대소문자를 규격서의 표와 정확히 비교. 다른 버전 파일이라면 해당 버전의
  디코더(HTML)를 사용하세요.

### 4.4 `stereo_mode=mid_side인데 channels가 2가 아닙니다`

- **원인**: `stereo_mode=mid_side`인데 `channels`가 1이거나 3 이상.
- **자주 있는 실수**: 헤더를 손으로 편집하다 `channels` 값만 바꾸고 `stereo_mode`는 그대로 둠
  (또는 반대). mid_side는 정확히 2채널(스테레오)에서만 의미가 있는 변환입니다.
- **해결**: 둘 중 하나를 맞추세요 — mono면 `stereo_mode=discrete`로, 진짜 스테레오면
  `channels=2`로.

### 4.5 `청크 N의 백레퍼런스가 유효하지 않습니다 (참조 대상=X)`

- **원인**: STREAM 안의 `=X` (백레퍼런스) 한 줄이, 존재하지 않거나 아직 나오지 않은 청크
  번호를 가리킴.
- **자주 있는 실수** (STREAM을 손으로 편집할 때 가장 흔한 사고):
  - 청크는 **항상 2줄**(마커 줄 + 데이터 줄)입니다. 이 중 한 줄만 지우거나 추가하면 그
    아래 모든 청크의 마커-데이터 짝이 밀려서 전부 어긋납니다.
  - `@CHUNK` 번호를 손으로 바꾸다 순서가 꼬임
- **해결**: STREAM 블록은 되도록 손으로 편집하지 마세요. 편집이 꼭 필요하면 청크를 2줄 단위로
  통째로 지우거나 복사하고, `@CHUNK` 번호가 0부터 순서대로 빠짐없이 이어지는지 확인하세요.

### 4.6 재생 화면에 "✗ 손상됨" 배지가 뜸 (오류 메시지는 없지만 체크섬이 안 맞음)

- **원인**: `[STREAM]` 블록의 내용이 저장되거나 전송되는 동안 미세하게 바뀜.
- **자주 있는 실수** (에러 메시지 없이 조용히 깨지는 가장 흔한 원인들):
  - **워드프로세서의 자동 교정**: 스마트 따옴표(`"`→`"`), 하이픈을 엔대시로 바꾸는 기능 등이
    STREAM 데이터의 문자를 바꿔버림 → 반드시 일반 텍스트 편집기(메모장, VS Code 등)만 사용
  - **줄 끝 공백 자동 삭제**: 일부 에디터/Git 설정이 "줄 끝 공백 자동 제거"를 켜두면 데이터
    일부가 잘릴 수 있음(단, 일반적인 줄바꿈 방식 차이(LF/CRLF)는 이 규격이 자동으로
    정규화하므로 문제되지 않습니다 — 문제가 되는 건 **글자 자체**가 바뀌거나 지워지는 경우입니다)
  - **일부만 복사됨**: 미리보기 상태(14줄만 보이는 상태)에서 복사 버튼을 누르지 않고 화면을
    직접 드래그해서 복사 — 미리보기는 일부러 잘려 보이므로 이렇게 복사하면 뒷부분이 통째로
    누락됩니다. 반드시 **"📋 텍스트 복사" 버튼**을 사용하세요(항상 전체 텍스트를 복사합니다).
- **해결**: "복사" 버튼으로 다시 복사하거나, `.jta` 파일 자체를 다시 업로드해서 재생해보세요.
  체크섬이 유효(✓)로 뜬다면 문제 없는 것입니다.

### 4.7 `⚠ 디코딩 실패: 지원되지 않는 오디오 형식일 수 있습니다`

- **원인**: 이건 `.jta`가 아니라 **원본 mp3/wav 업로드 단계**에서 나는 오류입니다. 브라우저가
  그 오디오 파일 자체를 못 읽는 것입니다.
- **자주 있는 실수**: DRM이 걸린 파일, 손상된 다운로드 파일, 아주 특이한 코덱으로 인코딩된
  wav(예: 일부 ADPCM 압축 wav)를 업로드.
- **해결**: 다른 mp3/wav로 시도하거나, 오디오 편집 프로그램에서 표준 PCM wav로 다시
  내보내서 업로드하세요.

### 4.8 MP3 다운로드 버튼이 비활성화되어 있음

- **원인**: 에러가 아니라 안내 문구입니다 — MP3 인코더(lamejs)를 인터넷에서 불러오지
  못했을 때 자동으로 비활성화됩니다.
- **해결**: 인터넷 연결을 확인하고 페이지를 새로고침하세요. WAV 다운로드는 인터넷 없이도
  항상 동작합니다.

---

## 5. 초보자가 제일 많이 하는 실수 TOP 10

1. **미리보기 화면을 직접 드래그해서 복사** → 반드시 "📋 텍스트 복사" 버튼 사용 (4.6절)
2. **닫는 태그의 슬래시(`/`)를 빼먹음** (`[HEADER]`인데 `[HEADER]`로 닫으려 함) → 2절 표 참고
3. **태그 대소문자를 다르게 씀** (`[header]`, `[Header]`) → 태그는 대문자로 정확히
4. **워드프로세서(한글, Word 등)로 `.jta`를 열고 저장** → 스마트 따옴표 자동 교정 때문에 파일이
   깨집니다. 항상 일반 텍스트 편집기를 쓰세요
5. **STREAM 블록의 청크를 한 줄만 지움** (마커 줄과 데이터 줄이 항상 짝이라는 걸 잊음) → 4.5절
6. **`codec`/`stereo_mode` 값을 소문자로 바꿔 씀** → 대소문자까지 정확히 일치해야 함
7. **품질(bit_depth)을 무조건 최고(20bit)로만 씀** → 파일이 base64(MP3)보다 커질 수 있음.
   텍스트로 공유할 목적이면 12bit 이하를 권장 (3절)
8. **인코딩 결과를 확인하지 않고 바로 배포** → "체크섬" 배지가 ✓인지 항상 재생해서 확인 (6절)
9. **원본 파일 저작권 확인 안 함** → 테스트할 땐 본인이 녹음했거나 라이선스가 명확한 파일만 사용
10. **여러 `.jta` 조각을 이어붙이려고 시도** → `.jta`는 파일 하나가 완결된 단위입니다. 여러
    파일의 STREAM만 잘라 붙이면 청크 번호·체크섬이 전부 깨집니다. 이어붙이고 싶다면 원본
    오디오를 먼저 합친 뒤 통째로 다시 인코딩하세요.

---

## 6. 체크섬으로 내 파일이 멀쩡한지 스스로 확인하기

`.jta` 파일 맨 끝 `[END]` 블록에 있는 `checksum=CRC32:XXXXXXXX`는 `[STREAM]` 블록 전체로
계산된 값입니다. 이 도구는 재생 직전 이 값을 다시 계산해서 비교하고, 결과를 배지로 보여줍니다:

- **✓ 유효**: STREAM 내용이 인코딩 당시와 완전히 동일함 — 안심하고 배포하세요.
- **✗ 손상됨**: 어딘가 한 글자라도 바뀌었다는 뜻입니다. 소리는 나올 수도 있지만(뒷부분이
  깨지거나 잡음이 섞일 수 있음), 원본과 100% 같다는 보장이 없습니다.

**GitHub에 올리기 전에는 반드시 다시 업로드/붙여넣기해서 "✓ 유효" 배지를 확인하는 습관을
들이세요.** 이 한 단계만으로 위에서 설명한 실수의 90%를 미리 잡아낼 수 있습니다.

---

## 7. GitHub에 올리기 전 체크리스트

- [ ] 파일 첫 줄이 정확히 `#JTA1.0`인가?
- [ ] `[HEADER]`~`[/HEADER]`, `[META]`~`[/META]`, `[STREAM]`~`[/STREAM]`, `[END]`~`[/END]`
      네 쌍 모두 존재하는가?
- [ ] "전체 보기" 상태에서 "📋 텍스트 복사" 버튼으로 복사했는가? (미리보기 화면 드래그 아님)
- [ ] 일반 텍스트 편집기로만 열고 저장했는가? (한글/Word 등 워드프로세서 사용 안 함)
- [ ] 다시 업로드/붙여넣기해서 재생해봤고 "체크섬: ✓ 유효"를 확인했는가?
- [ ] 들어봤을 때 손실률(RMS)이 용도에 맞게 만족스러운가?
- [ ] 원본 오디오의 저작권/라이선스에 문제가 없는가?

이 7가지만 확인하면 "왜 이게 안 열리지?"류의 이슈를 GitHub 저장소에 남기지 않을 수 있습니다.

---

## 8. 자주 묻는 질문 (FAQ)

**Q. `.jta` 파일을 GitHub README에 통째로 붙여넣어도 되나요?**
A. 네, 그게 이 포맷의 목적입니다. 다만 파일이 너무 길면 README 가독성이 떨어지니, 짧은
효과음/보이스 메모 정도에 붙여넣고, 긴 음원은 저장소에 `.jta` 파일로 첨부한 뒤 링크하는 걸
권장합니다.

**Q. 다른 사람이 만든 `.jta`를 받았는데 codec 오류가 납니다.**
A. 그 사람이 더 새 버전(예: v1.4)의 인코더로 만들었을 가능성이 큽니다. 규격서의 codec
표에서 해당 이름이 어느 버전에서 추가됐는지 확인하고, 그 버전 이상의 디코더(HTML)를
사용하세요. 오래된 디코더로 새 codec을 강제로 열 수 없게 만든 것은 의도된 안전장치입니다
(조용히 깨지는 대신 명확한 오류를 내기 위함).

**Q. STREAM 데이터를 손으로 고쳐서 소리를 편집할 수 있나요?**
A. 이론적으로는 가능하지만 권장하지 않습니다. 청크 하나가 정확히 2줄이라는 규칙, 백레퍼런스
번호, 체크섬까지 전부 손으로 맞춰야 해서 실수하기 매우 쉽습니다. 소리 자체를 바꾸고 싶다면
원본 오디오를 오디오 편집 프로그램에서 수정한 뒤 다시 인코딩하는 것이 훨씬 안전합니다. 다만
`[META]`의 제목·아티스트 같은 메타데이터는 손으로 고쳐도 안전합니다(체크섬은 STREAM 블록만
검사하기 때문입니다).

**Q. 체크섬이 안 맞으면 무조건 못 듣나요?**
A. 아닙니다. 어느 정도까지는 재생됩니다(어디가 깨졌는지에 따라 다름). 다만 "원본과 100%
같다"는 보장이 없다는 뜻이므로, 배포용으로는 반드시 ✓ 유효 상태로 만들어서 올리세요.
